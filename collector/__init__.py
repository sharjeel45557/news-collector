"""News collector: fetches RSS/Atom feeds from around the web and renders
an FT/WSJ-style static front page.

Stdlib-only by design so it runs anywhere (including GitHub Actions) with
no dependencies to install.
"""

__version__ = "1.0.0"
